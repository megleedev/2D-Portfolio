export function displayDialog (text, onDisplayEnd) {
    const dialogUI = document.getElementById("textbox-container");
}

// TODO: Start here when picking up the project again. 1:11:31 for references.